package presentation.other.extension.size

import androidx.compose.ui.unit.DpSize
import androidx.compose.ui.unit.dp

object ConstantSize {
    val homeButtonWidth = 240.dp

    val regularBorderWidth = 2.dp

    val trafficLightLampSize = DpSize(74.dp, 74.dp)
    val trafficLightLampButtonSize = DpSize(200.dp, 80.dp)

    val dividerStatisticHeight = 4.dp

    val emptySessionsListHeight = 260.dp
    val emptySessionsListSize = DpSize(width = 140.dp, height = 140.dp)
}